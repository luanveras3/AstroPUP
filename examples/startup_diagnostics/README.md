# 01 Startup Diagnostics

This example demonstrates:

```python
startup_report()
validate_remote_modes()
```

Expected output includes:

```text
Registered commands:
  0 reset
  1 state

Remote modes:
  0 ('reset', ...)
  1 ('state', ...)

Mode validation: OK
```

This example is good for a pit check before a competition run.
